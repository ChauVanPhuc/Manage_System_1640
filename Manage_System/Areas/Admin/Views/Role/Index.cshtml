@model IEnumerable<Manage_System.models.Role>

<!-- page css -->
<link href="~/student/assets/vendors/datatables/dataTables.bootstrap.min.css" rel="stylesheet">

<div class="header-sub-title">
    <nav class="breadcrumb breadcrumb-dash">
        <a class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
        <span class="breadcrumb-item active">Role</span>
    </nav>
</div>

<a class="btn btn-outline-info btn-sm add" asp-controller="Role" asp-action="Create" title="Create" style="margin-bottom: 20px">
    New Role <i class="bx bx-list-plus"></i>
</a>
<div class="card">
    <div class="card-body">
        <table id="data-table" class="table">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Description</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var account in Model)
                {
                    <tr>
                        <td>@account.Name</td>
                        <td>@account.Description</td>
                        <td >
                            <a class="btn btn-outline-success btn-sm edit" asp-area="Admin" asp-action="Edit" asp-route-id="@account.Id" title="Edit">
                                Edit <i class="fas fa-pencil-alt"></i>
                            </a>
                            
                        </td>
                        
                    </tr>

                }

            </tbody>
            <tfoot>

                <tr>
                    <th>Name</th>
                    <th>Description</th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>

@section Scripts{

    <!-- page js -->
    <script src="~/student/assets/vendors/datatables/jquery.dataTables.min.js"></script>
    <script src="~/student/assets/vendors/datatables/dataTables.bootstrap.min.js"></script>
}